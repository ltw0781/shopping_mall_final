<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="매퍼 인터페이스 경로" -->
<mapper namespace="com.shop.shopping.board.mapper.BoardMapper">

    <select id="list" resultType="Board">
        SELECT *
        FROM board
        ORDER BY board_id DESC
        LIMIT #{page.index}, #{page.rows}
    </select>

    <select id="read" resultType="Board">
        SELECT *
        FROM board
        WHERE board_id = #{boardId}
    </select>

    <insert id="insert">
        INSERT INTO board( category, title, user_id, content, created_at )
        VALUES ( #{category},  #{title}, #{userId}, #{content}, now())
    </insert>

    <!-- 게시글 수정 -->
    <update id="update">
        UPDATE board
            SET category = #{category}
                ,title = #{title}
                <!-- ,user_id = #{userId} -->
                ,user_id = #{userId}
                ,content = #{content}
                ,updated_at = now()
        WHERE board_id = #{boardId}
    </update>

    <delete id="delete">
        DELETE FROM board
        WHERE board_id = #{boardId}
    </delete>

    <!-- 데이터 개수 조회 - [검색] -->
    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM board
        ORDER BY created_at DESC
    </select>

</mapper>